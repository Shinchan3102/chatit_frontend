# Test Credentials

## Hosted URL:

```bash
https://chatit-frontend.vercel.app/sign-in
```

Use the following credentials to log in to the application:

- **Email**: test@gmail.com
- **Password**: testpassword

# Setup

## Getting Started

First, clone the repository:

```bash
git clone https://github.com/Shinchan3102/chatit_frontend
cd chatit_frontend
```

Install the dependencies and run locally:

```bash
npm install
npm run dev
```